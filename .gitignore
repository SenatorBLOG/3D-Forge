# dependencies
node_modules/

# builds
dist/
build/

# env / secrets
.env
.env.*
!.env.example

# dev-mode file persistence (local data when running without MongoDB)
server/.devdata/

# logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/

# claude local overrides (personal, not shared)
.claude/settings.local.json

# VS Code Local History extension — snapshots real files incl. .env (nearly leaked keys once)
.history/
